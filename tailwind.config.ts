import type { Config } from "tailwindcss";

const config: Config = {
  content: ["./index.html", "./form.html", "./src/**/*.{html,js,ts}"],
  theme: {
    extend: {},
  },
  plugins: [],
};

export default config;
